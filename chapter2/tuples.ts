///here we can fix the postition , length , and type of array

const tArray :[number, string, number] = [1,'sd',5]   ///if is fixed order and postions if we try to change the order it will show error

type user=[string,number]

const newUser:user = ['1',0]  

///but here we can use array .push pop shift unshift etc